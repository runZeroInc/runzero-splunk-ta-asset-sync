[assets://<name>]
sync_type = Determines which assets are imported from runZero on each sync. New: Only sync new assets found since the last import. Updated: Only sync assets that have been updated since the last import. All: Sync all assets, regardless of when they were found or updated.
search_filter = The search filter can be used to limit the types of assets returned by the asset sync. See the runZero Query Syntax documentation for additional details.
import_services = Enable or disable importing your service
batch_size = The number of assets to fetch during each request
global_account =
